import s from "./page.module.scss";

const Blogs = () => {
  const links = [{ title: "Blog", href: "#" }];

  return (
    <div>
        <h1>Admin dashboard</h1>
      
    </div>
  );
};

export default Blogs;
