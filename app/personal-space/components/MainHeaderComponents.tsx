import s from "./MainHeaderComponents.module.scss";
// import classNames from "classnames";

const MainHeaderComponents = () => {
  return (
    <div className={s.settings__page}>
      <h2 className={s.title}>Settings</h2>
    </div>
  );
};

export default MainHeaderComponents;
